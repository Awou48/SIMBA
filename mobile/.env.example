# Leave empty to auto-detect the backend on the machine running `expo start` (port 8000).
# Override only if the backend runs elsewhere, e.g. EXPO_PUBLIC_API_URL=http://192.168.0.104:8000
EXPO_PUBLIC_API_URL=
